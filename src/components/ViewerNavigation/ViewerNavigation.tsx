import React, { FC } from 'react';

type Props = {
  /** User privilages */
  editPrivileges?: boolean;
};

const ViewerNavigation: FC<Props> = () => <div>mav</div>;

export default ViewerNavigation;
