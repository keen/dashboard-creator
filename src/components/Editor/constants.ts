import { GridSize } from '../../types';

export const DROP_ELEMENT_ID = '__dropping-elem__';

export const INITIAL_GRID_SIZE: GridSize = {
  cols: 1,
  containerPadding: null,
  containerWidth: 1200,
  margin: [0, 0],
};
